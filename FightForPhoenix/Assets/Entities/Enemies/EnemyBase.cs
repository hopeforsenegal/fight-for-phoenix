using UnityEngine;

[CreateAssetMenu(fileName = "EnemyBase", menuName = "FightForPhoenix/EnemyBase", order = 0)]
public class EnemyBase : ScriptableObject {
    //entity attempts to dodge or not
    
}

